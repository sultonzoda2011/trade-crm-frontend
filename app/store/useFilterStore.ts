// Superseded by createTableStore() in ~/store/useTableStore
// Each page creates its own instance in a local store.ts file:
//   import { createTableStore } from '~/store/useTableStore'
//   export const useFilterTableStore = createTableStore()
